# TH-SEARCH-R1-009 release receipt

R1-009 is released and verified on the canonical Contractor alias. Model: GPT-6 Astra / High, USER-CONFIRMED. Only Contractor was edited. The dirty original checkout and unrelated NYC PR #60 were left untouched.

Baseline: `11fd0b6fd8f6a1fe5e40e48a34f6a578b67334f4`. Implementation PR #59 merged as `1e07521e5aa18b4ea8a6c99da5251b6d08ecd2a5`. Consent follow-up PR #61 tested head `3c1419e7bdfb903c7b3588b25fbb2ed0b78f13c2`, merged as `ca77548a762b07618c8a33b91c7a9eb81bc38a05`. Verified runtime deployment: `dpl_ixtozAw9Vo2yM1WzrMDMp6AiL1Mo`, `https://www.contractortrusthub.com`. This receipt is a subsequent documentation-only change; no future merge SHA is invented.

## Observed acceptance

- Austin and Dallas retain city, TX and HVAC. Their local query does not execute. The existing published TDLR TAC cohort supports state research only after the customer's explicit choice. The inspected source has no populated TAC city values.
- `browrd county fl` requires Apply Broward County. No statewide or corrected request runs before interaction. Exact and accepted-correction pages display the same 24 records in the tested window; every row carries Broward County, Florida recorded geography.
- The accepted-correction -> explicit Florida state research chain works, with correction history validated server-side. Query edits discard old consent; same-query Enter, refresh and history retain it.
- Native/API positive controls: Broward all-status roofing 1,013 credential records; NJ active Master Plumber 4,903; TX active TAC state cohort 17,757. These are source-window observations, not production constants. Palm Beach HVAC, exact CCC1332036 and a scoped valid zero pass. Record count and profile count remain distinct grains.
- Both consent paths show zero pre-click requests, followed by a non-prefetch request after keyboard selection. This includes the second correction-to-state choice. The first release's prefetch defect and red evidence remain recorded.

## Evidence and limits

`result.json` indexes 32 final settled desktop/mobile cases, 13 API/route checks, 5 row-level checks (24 displayed records each), source oracle, timings and screenshots. Widths 320/390/768/1280, keyboard, Trace, edits, refresh/history and stale-result clearing pass. The measured full-flow browser range is recorded in JSON; it is not a latency guarantee.

Thirty focused tests pass. Both city-dropping and typo-to-state mutations fail on the exact candidate, then restore byte-identically and pass. Existing state/metrics/publication/customer/claim checks, typecheck and production builds pass. Two unrelated legacy assertions remain failing on both isolated baseline and candidate: the hardcoded network total and obsolete homepage heading. Local database tests are skipped because the production credential is unavailable via authorized environment pull; live positive preview and Production tests are separately recorded. No lint command is configured.

Review was a separate self-review plus executable tests/mutations and the successful Vercel Agent Review check on PR61. No independent human review is claimed. The final scoped log check found no search errors, but did observe one unrelated existing public-profile lineage timeout with HTTP 200; all-log cleanliness is not claimed.

No data/schema/index writes, new paid resources, profile-publication changes, raw-query analytics or other-repository edits. Rollback is a reviewed Contractor-only corrective revert/deployment retaining no-silent-broadening containment; no database rollback. Broader Texas locality/service coverage, Contractor how-to recovery, other hubs and Search Reliability R1 overall remain open.
